<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ezen.www.repository.FileMapper">
    <insert id="JoinFiles">
        insert into joinfile(name, tell, report, uuid, save_dir, file_name, file_img, file_type, file_size)
        values(#{name}, #{tell}, #{report}, #{uuid}, #{saveDir}, #{fileName}, #{fileImg}, #{fileType}, #{fileSize})
    </insert>
</mapper>